spring:
  datasource:
    username: root
    password: raisanjeeb42
    url: jdbc:mysql://localhost:3306/movies
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

project:
  poster: posters/


base:
  url: "http://localhost:8080"